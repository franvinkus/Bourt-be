namespace Bourt.Enums
{
    public enum UserRole
    {
        Admin,
        Owner, 
        Customer,
    }

    public enum BookingStatus
    {
        Cancelled,
        Pending,
        Verifying,
        Confirmed,
        Completed,
    }
}
