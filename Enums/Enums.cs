namespace Bourt.Enums
{
    public enum UserRole
    {
        Admin,
        Owner, 
        Customer,
    }

    public enum BookingStatus
    {
        Cancelled,
        Pending,
        Confirmed,
        Completed,
    }
}
